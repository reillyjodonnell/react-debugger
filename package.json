{
  "name": "@react-debugger/core",
  "version": "0.0.16",
  "description": "React debugger for humans and AI agents - inspect components, set breakpoints, and understand React internals programmatically",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./react-bridge-client": {
      "import": "./dist/react-bridge-client.js"
    }
  },
  "files": [
    "dist",
    "bin"
  ],
  "bin": {
    "react-debugger-core": "bin/cli.js"
  },
  "scripts": {
    "build": "bun build src/index.tsx --outdir dist --target browser       && bun build src/cli.ts --outdir dist --target node       && bun build src/debugger-server.ts --outdir dist --target bun       && bun build src/mcp-server.ts --outdir dist --target bun       && bun build src/debugger-widget.tsx --outdir dist --target browser       && bun build src/react-bridge-client.ts --outdir dist --outfile dist/react-bridge-client.js --target browser       && mkdir -p dist/templates && cp src/templates/mcp.json dist/templates/mcp.json",
    "dev": "bun --watch src/index.tsx",
    "chrome": "/Applications/Google\\ Chrome.app/Contents/MacOS/Google\\ Chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug http://localhost:5173",
    "prepublishOnly": "bun run build",
    "typecheck": "tsc --noEmit",
    "local": "bun run build && npm pack && sudo npm i -g ./$(npm pack | tail -1) && bunx serve dist --cors -p 8080"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "keywords": [
    "react",
    "debugger",
    "agents"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "lucide-react": "^0.525.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "sourcemap-codec": "^1.4.8"
  }
}
